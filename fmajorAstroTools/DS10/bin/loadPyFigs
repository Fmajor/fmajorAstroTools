#!/bin/bash
last="${@: -1}"
scriptPath=$(python3 -c 'import fmajorAstroTools as f;import os.path as p;print(p.join(p.dirname(p.abspath(f.__file__)), "DS10/loadPyFigs.py"))')
if [[ ${last} == "--gui" ]]; then
    PY="ipython3 -i"
else
    PY="ipython3 "
fi

${PY} --no-banner -- ${scriptPath} $@
